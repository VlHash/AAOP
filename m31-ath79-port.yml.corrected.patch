diff --git a/.github/workflows/m31-ath79-port.yml b/.github/workflows/m31-ath79-port.yml
--- a/.github/workflows/m31-ath79-port.yml
+++ b/.github/workflows/m31-ath79-port.yml
@@ -252,3 +252,4 @@
               "files/etc/uci-defaults/99-air720"
               "files/etc/hotplug.d/net/95-air720-rndis"
+              "files/etc/hotplug.d/tty/95-air720-ppp"
               "files/usr/bin/air720-at"
@@ -283,5 +283,28 @@
             fi
           done
 
+          # ZIP extraction, Windows worktrees and web uploads may lose Git's
+          # executable bit. Force runtime script modes in the OpenWrt overlay
+          # and fail early if any expected Air720SL file is missing.
+          if [[ "$ENABLE_AIR720SL" == "true" ]]; then
+            air720_exec_paths=(
+              "files/etc/init.d/air720"
+              "files/etc/uci-defaults/99-air720"
+              "files/etc/hotplug.d/net/95-air720-rndis"
+              "files/etc/hotplug.d/tty/95-air720-ppp"
+              "files/usr/bin/air720-at"
+              "files/usr/bin/air720-bind"
+              "files/usr/bin/air720-ports"
+              "files/usr/bin/air720-ppp"
+              "files/usr/bin/air720-rndis6"
+              "files/usr/libexec/air720-chat-connect"
+            )
+            for rel in "${air720_exec_paths[@]}"; do
+              test -f "$OPENWRT_DIR/$rel"
+              chmod 0755 "$OPENWRT_DIR/$rel"
+              test -x "$OPENWRT_DIR/$rel"
+            done
+          fi
+
           if [[ "$ENABLE_AIR720SL" == "true" ]]; then
             test -f "$OPENWRT_DIR/$AIR720_KERNEL_PATCH"
@@ -757,12 +757,22 @@
             test -x "$rootfs/etc/init.d/air720"
             test -x "$rootfs/etc/uci-defaults/99-air720"
             test -x "$rootfs/etc/hotplug.d/net/95-air720-rndis"
+            test -x "$rootfs/etc/hotplug.d/tty/95-air720-ppp"
+            test -x "$rootfs/usr/bin/air720-at"
+            test -x "$rootfs/usr/bin/air720-bind"
             test -x "$rootfs/usr/bin/air720-ppp"
             test -x "$rootfs/usr/bin/air720-rndis6"
             test -x "$rootfs/usr/bin/air720-ports"
             test -x "$rootfs/usr/libexec/air720-chat-connect"
             grep -q "proto='ppp'" "$rootfs/usr/bin/air720-ppp"
             grep -q "pppname='air720-cellular'" "$rootfs/usr/bin/air720-ppp"
+            grep -q "noipv6 115200" "$rootfs/usr/bin/air720-ppp"
+            grep -q "prepare)" "$rootfs/usr/bin/air720-ppp"
+            grep -Fq "uci -q delete network.cellular.apn || true" "$rootfs/usr/bin/air720-ppp"
+            grep -Fq "uci -q delete network.lan.ip6assign || true" "$rootfs/usr/bin/air720-rndis6"
+            grep -Fq "uci -q delete dhcp.cellular6 || true" "$rootfs/usr/bin/air720-rndis6"
             grep -q "CGDCONT=5" "$rootfs/usr/bin/air720-rndis6"
             grep -q "CGACT=1,5" "$rootfs/usr/bin/air720-rndis6"
+            grep -q "air720-ppp prepare" "$rootfs/etc/init.d/air720"
+            grep -q "air720-ppp prepare" "$rootfs/etc/uci-defaults/99-air720"
           else
